# claude-code-config

Claude Code 글로벌 설정(`~/.claude/`)을 여러 머신에서 공유하기 위한 개인 설정 저장소입니다. 이 저장소 자체가 작업 대상이며, 일반적인 애플리케이션 코드베이스가 아닙니다.

## 이 저장소가 하는 일

- `~/.claude/`에 로드되는 글로벌 설정(규칙·스킬·커맨드·서브에이전트)을 버전관리합니다.
- 머신 간 동기화는 **symlink** 기반입니다. 자세한 사항은 `README.md` 참고.

## 디렉터리 구조

| 경로 | 글로벌 매핑 | 목적 |
|-|-|-|
| `rules/` | `~/.claude/rules/` | 모든 작업에 횡단 적용되는 행동 규칙 |
| `skills/` | `~/.claude/skills/` | 재사용 가능한 워크플로 단위 |
| `commands/` | `~/.claude/commands/` | 슬래시 커맨드 정의 |
| `agents/` | `~/.claude/agents/` | 서브에이전트 정의 |
| `settings.json` | `~/.claude/settings.json` | Claude Code 런타임 설정 |
| `README.md` | — | 설치·동기화·설계 철학 |
| `DECISIONS.md` | — | 의식적으로 배제한 항목과 재평가 트리거 |
| `AUDIT.md` | — | 환경 자가 검토용 프롬프트 모음 |

## 작업 시 유의 사항

- 이 저장소의 파일 변경은 **로컬 Claude Code 동작에 즉시 영향**을 줍니다(symlink로 연결되어 있을 가능성이 큼). 규칙·스킬 수정은 다른 코드 수정보다 신중하게.
- 새 규칙·스킬·에이전트 추가 전, `DECISIONS.md`의 평가 기준 4단계를 통과하는지 먼저 확인합니다.
- 배제 결정도 자산입니다. 도입하지 않기로 한 항목은 `DECISIONS.md`의 "Rejected" 또는 "Deferred" 섹션에 재평가 트리거와 함께 기록합니다.
- `README.md`·`DECISIONS.md`·`AUDIT.md`는 사람이 읽는 문서로, 글로벌 `~/.claude/`에는 복사되지 않습니다.

## 설계 철학 요약

자세한 내용은 `README.md`의 "설계 철학" 섹션 및 `DECISIONS.md` 참고.

1. 단순성 우선 — 모델이 이미 잘 하는 것은 규칙으로 만들지 않음
2. 가정의 명시성 — 모든 구성 요소는 "모델이 이것을 혼자 못 한다"는 가정을 인코딩함
3. 배제의 기록 — 도입하지 않기로 한 결정도 문서화
4. 외부 도구 선호 — 결정론적 검증이 가능하면 규칙보다 hooks로 연결
