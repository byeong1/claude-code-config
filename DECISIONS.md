# DECISIONS.md

이 문서는 `claude-code-config`에 **의식적으로 포함하지 않기로 결정한 항목**과 그 이유를 기록합니다.

하네스 엔지니어링의 메타 원칙은 다음과 같습니다.

> *"Every component in a harness encodes an assumption about what the model can't do on its own."*
> — Anthropic, Harness Design for Long-Running Claude Agent Applications

따라서 **규칙·스킬·에이전트를 추가하지 않는 것도 하나의 설계 결정**입니다. 이 문서는 그 결정들을 남겨, 미래에 재평가할 수 있는 근거를 제공합니다.

## 평가 기준

새로운 규칙/스킬/에이전트 도입 여부를 판단할 때 다음 질문을 순서대로 적용합니다.

1. **현재 사용 중인 모델이 이 작업을 이미 충분히 잘 하는가?**
   - YES → 추가하지 않음
   - NO → 2번으로
2. **Claude Code의 기본 하네스가 이 문제를 이미 해결하는가?**
   - YES → 추가하지 않음
   - NO → 3번으로
3. **외부 결정론적 도구(린터·타입체커·테스트)가 대체 가능한가?**
   - YES → hooks로 연결, 규칙으로 만들지 않음
   - NO → 4번으로
4. **토큰·컨텍스트 비용 대비 품질 개선이 분명한가?**
   - YES → 추가
   - NO → 추가하지 않음

## 의식적으로 배제한 항목

### 1. 전역 Feedback Loop 규칙

**상태**: Rejected (배제)

**배제 이유**:
- 현재 기준 모델(Claude Opus 4.6 등)은 작업 완료 후 자체 반성·재검토를 상당 수준 수행함
- 개발 워크플로 내 외부 피드백 신호(타입체커, 린터, 테스트)가 존재하는 경우가 많음 → 이것이 더 싸고 확정적인 evaluator 역할
- 전역 규칙으로 강제할 경우 매 작업마다 토큰·컨텍스트 세금이 발생하며, 대부분의 일상 작업에서는 중복 작업이 됨
- 컨텍스트 위생은 `/clear` 습관으로 능동 관리하고 있어, 긴 세션에서 오류 누적 문제가 상대적으로 적음

**전제 가정 (재평가 트리거)**:
- 모델이 "다 됐습니다"라고 보고한 뒤 실제로는 틀리는 경우가 반복적으로 목격되면 재검토
- 외부 검증 신호가 없는 작업(문서, 설계, 타입 없는 스크립트)이 주 업무가 되면 재검토
- 긴 세션에서 clear 없이 작업이 누적되는 사용 패턴으로 바뀌면 재검토

**도입하게 된다면의 방향**:
- 전역 규칙이 아닌 hooks 기반 외부 신호화 (`PostToolUse`에서 lint/typecheck 실행, 실패 시에만 모델이 반응)
- 또는 고위험 스킬(예: `commit`, 배포 관련)에만 선택적 삽입

---

### 2. 전역 Verification Checklist 규칙

**상태**: Rejected (배제)

**배제 이유**:
- Feedback Loop과 같은 이유로 모델 자체 능력과 외부 도구로 대체됨
- `action-guard`가 **작업 시작 시점**의 가드를 제공하고 있어, 시작 가드 + 모델 자체 검증 + 외부 도구의 조합이 이미 사실상의 검증 체계를 구성

**재평가 트리거**:
- 동일 실수가 여러 작업에서 반복 관찰될 때
- 검증이 필요한 작업이 정형화되어 체크리스트화 가치가 생길 때

---

### 3. Context Reset / 아티팩트 핸드오프 규칙

**상태**: Rejected (배제)

**배제 이유**:
- 수동으로 `/clear`를 능동 사용하는 습관이 이미 존재 → Anthropic 문서 P3(Context Resets) 원칙을 사람이 직접 실천 중
- 규칙으로 자동화할 필요성이 낮고, 자동화는 오히려 원치 않는 리셋을 유발할 위험이 있음

**재평가 트리거**:
- clear 타이밍을 놓쳐 컨텍스트 오염이 반복 발생할 때
- 작업 전환 시점을 자동 감지할 필요가 생길 때

---

### 4. 도메인별 오케스트레이터 스킬 (기획·빌드·분석 등)

**상태**: Deferred (보류)

**보류 이유**:
- 본 저장소는 **개인 런타임 가드레일** 레이어를 목적으로 함
- 도메인별 파이프라인 자동화는 다른 레이어(애플리케이션)에 속함
- 필요한 시점에 별도 저장소 또는 별도 디렉토리로 분리하여 관리하는 것이 레이어 혼재를 방지

**재평가 트리거**:
- 특정 반복 작업(예: 일정한 형태의 기획 → 빌드 파이프라인)이 주간 단위로 반복될 때
- 개인 사용을 넘어 팀/조직 공유가 필요해질 때

---

### 5. `$$변수` 수집 프로토콜 / Quick Help UX

**상태**: Deferred (보류)

**보류 이유**:
- 현재 스킬 수가 적어(약 5개) 일관된 변수 규격의 이점보다 규칙 오버헤드가 큼
- 대화형 UX는 Claude Code 자체가 이미 제공

**재평가 트리거**:
- 스킬이 10개 이상으로 늘어나 변수 규격 통일이 필요해질 때
- 여러 기기에서 비개발자와 함께 스킬을 공유해야 할 때

---

### 6. Symlink와 `/sync-settings` 병행 운영

**상태**: Adopted (채택) — 단, "왜 둘 다인가"를 기록함

**결정**:
일상 동기화는 **symlink**로 자동화하고, 머신 간 diff 확인·실험 승급·머신별 분기 같은 고급 운영은 **`/sync-settings`** 스킬을 사용한다.

**결정 이유**:
- Symlink만 사용하면: 자동화는 최고이지만 로컬 실험 공간이 사라지고, pull 즉시 모든 변경이 검토 없이 반영됨
- sync-settings만 사용하면: 양방향 게이트는 좋으나 매번 명시적 명령이 필요해 망각 위험
- 두 도구는 **다른 가치**를 제공하므로 공존 가능
  - Symlink: 일상 자동 동기화
  - sync-settings: 명시적 게이트가 필요한 상황(머신 간 비교, 실험 승급, 부분 적용)

**전제 가정 (재평가 트리거)**:
- Symlink만으로 충분하다고 느껴지면 sync-settings 제거 고려
- 반대로 머신 간 분기가 중요해지면 symlink를 제거하고 sync-settings 단독 운영 고려

## 재평가 주기

- **분기마다 1회** 본 문서를 훑으며 재평가 트리거가 발생했는지 점검
- Claude / Claude Code 메이저 버전 업데이트 시 "모델 능력 가정"이 바뀌었는지 확인 (Anthropic 문서 P10)
- 재평가 결과 도입을 결정한 항목은 본 문서에서 제거하고 규칙/스킬로 이동

## 이 문서의 존재 이유 자체에 대해

규칙·스킬이 추가될 때는 자연스럽게 기록(커밋, 파일)이 남지만, **무언가를 "넣지 않기로 한 결정"은 기록이 남지 않는다**. 그래서 6개월 후 "왜 없지?"를 자문할 때 답이 없어 중복 논의가 발생한다.

이 문서는 **배제의 근거를 버전관리**하여, 미래의 자기 자신(그리고 Claude)이 동일한 결론에 반복 도달하지 않게 하는 장치다.

## 참고

- Anthropic — Harness Design for Long-Running Claude Agent Applications: <https://www.anthropic.com/engineering/harness-design-long-running-apps>
