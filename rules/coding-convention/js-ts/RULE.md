# JavaScript / TypeScript Convention

**Comments:**
- Use `/** */` (JSDoc) or `/* */` for comments.
- Avoid `//` single-line comments.

**Functions:**
- Prefer arrow functions (`const fn = () => {}`). Avoid `function` declarations.

**Control flow:**
- Prefer `if/else` over `switch`. Avoid `switch` statements.
