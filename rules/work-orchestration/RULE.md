# Work Orchestration Rule

When a code modification or implementation request requires changes to **2 or more files**, invoke the `/work-orchestration` skill before writing any code.

- If **2+ files** need modification → invoke the `/work-orchestration` skill.
- If only **1 file** needs modification → skip the skill and proceed directly.
